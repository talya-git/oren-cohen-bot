name: Update Properties from Sehel

# DISABLED - scraping runs from local machine (run_persistent.py)
# GitHub Actions can't login to Sehel due to 2FA

on:
  workflow_dispatch:  # only manual trigger

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "This workflow is disabled. Scraping runs from local machine."
